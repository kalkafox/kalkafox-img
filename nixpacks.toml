[phases.setup]
nixPkgs = ['...', 'nodejs', 'python']

[phases.build]
cmds = ['npm install --prefix frontend", "npm --prefix frontend run build', 'cargo build --release']

[start]
cmd = 'target/release/kalkafox-img'